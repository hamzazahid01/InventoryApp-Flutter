rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Flat cover files: products/{productId}.jpg
    match /products/{fileName} {
      allow read, write: if true;
    }
  }
}
